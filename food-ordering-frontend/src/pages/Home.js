import React from "react";
import {Header} from "../components/Header";
import {Menu} from "../components/food/Menu";

export const Home = () => {
    return (
        <div>
            <Header/>
            <Menu/>
        </div>
    );
};